{
 "notes": [
	"JSON files always start with either an array [] or an object {}",
	"JSON files cannot have comments, so put any comments in this array",
	"Commas go between items in an array or object, but there should never be a trailing Commas",
	"All property names must be in quotation marks",
	"All string qutation marks must be double quotes",
	"Use a Linter like jsonlint.com to check json"

	]

}